
# Legacy Studio Notes Generator

This folder contains the older CSV + Python static article generator.

**Important:** its generated header/theme templates predate the current DesignLab
website. The existing live Studio Notes pages are already generated, so this tool
is preserved only as a reference and should not be run without updating it first.
